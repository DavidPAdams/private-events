<h1>All Events</h1>

<div class="row">
  <div class="col-md-8 col-md-offset-2">
    <p>Upcoming events:</p>
    <ul><%= render @events.upcoming %></ul>
    <p>Past events:</p>
    <ul><%= render @events.past %></ul>
  </div>
</div>
